---
"@jikjo/ui-kit": minor
"@jikjo/core": minor
---

Add `exportToHtml` and `exportToMarkdown` utilities. No need to install `@lexical/html` or `@lexical/markdown` separately — they are now bundled as dependencies of `@jikjo/core`.
