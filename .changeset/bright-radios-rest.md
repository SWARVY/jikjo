---
"@jikjo/ui-kit": minor
---

Extract top toolbar styles into CSS classes and expose them as CSS custom properties (`--jikjo-toolbar-border`, `--jikjo-btn-*`). The toolbar background, button colors, hover/active states, and border are now fully customizable without overriding component styles.
